#cloud-config
users:
  - name: clawx
    gecos: tank-claw-os agent user
    shell: /bin/bash
    lock_passwd: true
    ssh_authorized_keys:
      - ssh-ed25519 REPLACE_WITH_YOUR_PUBLIC_KEY tank-claw-os

runcmd:
  - [loginctl, enable-linger, clawx]
